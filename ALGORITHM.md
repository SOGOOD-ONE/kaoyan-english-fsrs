# 核心算法

## 1. 系统词库导入

系统词库由启动时的词表文件自动导入：

`xlsx` / `csv` / `json` -> `import_workbook()` -> `Vocabulary` + `Word` + `VocabularyWord`

同一个规范化单词只保留一份 `Word` 记录，多个词库只建立关联关系。

## 2. 今日计划生成

每日计划的核心入口是 `get_or_create_plan(user)`。

1. 先根据当前启用词库计算 `selected_word_ids`
2. 再根据昨日新词复习完成情况计算 `mandatory_word_ids`
3. 用 `mandatory_completed_ids` 得出今日已完成数量
4. 统一回写 `DailyPlan`

核心原则：

- 词库变化后，计划必须按当前词库重新计算
- 已完成的数量只按当天真实完成的复习记录统计

## 3. 新词学习队列

新词学习分两层：

1. 后端 `/study/new-queue` 生成候选词
2. 前端按 `step` 和 `dueStep` 进行重复调度

答错后不会直接推进完成，只会把该词重新放回队列；答对并满足条件后才进入 FSRS 复习。

## 4. FSRS 卡片修正

本地卡片在进入 FSRS 之前会先做一次归一化：

- `new` 卡如果是 `stability = 0` 的旧格式，会重置成空卡
- 非 `new` 卡如果状态异常，也会回退为空卡

这样可以避免 `Invalid memory state` 直接中断学习流程。

## 5. 词库删除

删除只允许用户自己的词库。

删除流程：

1. 删除词库和关联关系
2. 保留单词主表和学习记录
3. 如果当天已经有计划，就走同一套核心计划引擎重算

这样删完词库后，今天的任务数不会卡在旧值上。
